<div>
  <div class= 'container'>
  <h1>Watch List</h1>
  <h2>List</h2>
  <ul>
    <li>
      <% @lists.each do |list| %>
        <%= link_to list.name, list_path(list) %><br>
      <% end %>
    </li>
  </ul>
    <hr>
    <p><%= link_to'<button type="button">Add new List</button>'.html_safe, new_list_path(@list) %></p>
  </div>
</div>
